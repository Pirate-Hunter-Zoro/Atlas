---
kind: question
title: Where is the work planned?
---

I read `README.md` first, as it asks. It tells me what this repository **is** — a Go module of LeetCode solutions, one package per problem, `testutil.RunTestHelper` driving every table-driven test, shared `helpermath` and `datastructures` underneath — and it tells me how to add a problem.

What it does not tell me is what to add.

There is no task list in it, no planning document, no issue tracker, no companion repository holding the narrative. There is no `HANDOFF.md` at the root either. The `references/` directory that would hold problem writeups is gitignored, so I cannot read a plan out of it.

I am not going to survey `leetcode/` and pick a problem for you. Choosing the work in somebody else's project is the one thing I am explicitly not allowed to do here, and a problem I found interesting is not a problem you were going to solve.

So:

**Where is the next piece of work written down — a problem number, a list, a document, another repository?**

If the answer is "nowhere, I just pick one when I sit down", say that and name the one you want. Either is a fine answer. I need it before I can say what file to touch.

*Answer on the board — write on this card or type it. Do not go to the terminal.*
