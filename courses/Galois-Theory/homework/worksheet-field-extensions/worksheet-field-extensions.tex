% ===========================================================================
%  Worksheet --- Field Extensions and the Ring F[x]
%
%  THE WRITE-UP, and it is the assistant's to type.
%    The assistant transcribes each question or topic faithfully and emits an
%    empty, marked solution region beneath it. The student does the
%    mathematics; the assistant typesets what they agreed was right.
%
%  ONE `problem` ENVIRONMENT PER LETTERED PART, each with its own matched
%  SOLUTION / END SOLUTION pair. `board hw status` reads those markers to say
%  what is written up and what is next, and it pairs them by label: a part
%  folded into its parent's statement is a part the board cannot see, and an
%  unmatched opener swallows every region below it. A sheet that states 3 and 6
%  as single problems reported this one as "3, 6 still to write up" with all of
%  6 already typeset underneath.
% ===========================================================================
\documentclass[11pt]{article}
\usepackage{coursemacros}

\title{Worksheet --- Field Extensions and the Ring $F[x]$}
\author{Mikey Ferguson}
\date{\today}

\begin{document}
\maketitle

% One problem/solution pair per thing worked, in the sheet's own order. A
% region stays empty until the answer in it is agreed correct.

\begin{problem}{1(a)}
  \emph{Quotients of $F[x]$.} Let $F$ be a field and let $f \in F[x]$ be
  nonconstant of degree $n$. Set $A = F[x]/(f)$. Show that
  $\dim_F A = n$.
\end{problem}

% ===== SOLUTION 1(a) =====
% Transcribed from the student's slate, 2026-09-22 (pages t0054-r58 to r64,
% cards 0069-0074). Spanning by division with remainder, independence by
% comparing degrees: their route, their order, nothing added.
% Three reason-corrections, made here rather than left to the student:
%   - The page states division with remainder as "deg r < deg f" and adds
%     "(or r = 0)" beside it; the zero case is part of the statement, so it is
%     stated inside it.
%   - The independence half says "by assumption h != 0", but the stated
%     assumption was hbar = 0bar, which says nothing about h. The hypothesis
%     actually used is the contradiction hypothesis, so it is declared first:
%     suppose some c_i is non-zero.
%   - The page writes "hbar = fbar or h in (f)". Only the second is usable,
%     since fbar is always 0bar; the write-up keeps that one.
% The zero polynomial is written $0$ in $F[x]$; the page's $0_{k[x]}$ names a
% field $k$ that does not occur in this problem.
\begin{solution}
  Write $\overline{g} = g + (f)$ for the class of $g \in F[x]$ in $A$.

  \textbf{The classes $\overline{1}, \overline{x}, \dots, \overline{x^{n-1}}$
  span $A$ over $F$.} Let $\overline{g} \in A$. By division with remainder in
  $F[x]$ there are $q, r \in F[x]$ with
  \[
    g = qf + r, \qquad \text{where } r = 0 \text{ or } \deg r < \deg f = n .
  \]
  Passing to classes and using $\overline{f} = \overline{0}$,
  \[
    \overline{g} = \overline{q}\,\overline{f} + \overline{r} = \overline{r} .
  \]
  Since $r = 0$ or $\deg r < n$, we may write $r = \sum_{i=0}^{n-1} c_i x^i$
  with $c_0, \dots, c_{n-1} \in F$, and therefore
  \[
    \overline{g} = \overline{r} = \sum_{i=0}^{n-1} c_i \overline{x^i} .
  \]
  So every element of $A$ is an $F$-combination of the $n$ classes
  $\overline{1}, \overline{x}, \dots, \overline{x^{n-1}}$.

  \textbf{They are linearly independent over $F$.} Suppose
  $c_0, \dots, c_{n-1} \in F$ satisfy
  $\sum_{i=0}^{n-1} c_i \overline{x^i} = \overline{0}$, and set
  $h = \sum_{i=0}^{n-1} c_i x^i \in F[x]$, so that $\overline{h} = \overline{0}$.
  Suppose, for a contradiction, that some $c_i$ is non-zero. Then $h \neq 0$ and
  $\deg h \leq n-1$.

  From $\overline{h} = \overline{0}$ we get $h \in (f)$, so there is some
  $g \in F[x]$ with $gf = h$. Since $h \neq 0$, also $g \neq 0$. Degrees add for
  non-zero polynomials over a field, so
  \[
    \deg h = \deg g + \deg f = \deg g + n \geq n ,
  \]
  which contradicts $\deg h \leq n-1$. Hence every $c_i = 0$.

  The $n$ classes $\overline{1}, \overline{x}, \dots, \overline{x^{n-1}}$ span
  $A$ and are linearly independent over $F$, so they are a basis and
  $\dim_F A = n$.
\end{solution}
% ===== END SOLUTION 1(a) =====

\begin{problem}{1(b)}
  \emph{Quotients of $F[x]$.} Let $F$ be a field and let $f \in F[x]$ be
  nonconstant of degree $n$. Set $A = F[x]/(f)$. Show that if $A$ is an
  integral domain, then $A$ is a field.
\end{problem}

% ===== SOLUTION 1(b) =====
% Transcribed from the student's slate, 2026-09-22 (page t0054-r69, cards
% 0075-0078). Their route: fix a non-zero class, multiply by it, injective from
% the domain hypothesis, surjective from rank-nullity, then feed 1bar.
% Two additions, both citations the argument spends and the page leaves
% unnamed rather than steps the student did not take:
%   - The page says "finite dimensional space"; which dimension is 1(a), so
%     $\dim_F A = n$ is cited where rank-nullity is invoked.
%   - Rank-nullity needs $T$ linear, so $T$ is stated as $F$-linear and the
%     one-line check is given.
% One clause on the page is dropped as doing no work: "if g1bar, g2bar != 0,
% both sides are not 0". The subtraction covers every case. The page's opening
% lines h = gf + r, r != 0, r = sum c_i x^i are the abandoned attempt to build
% the inverse by hand and are not part of this proof.
\begin{solution}
  Assume $A$ is an integral domain. Then $A$ is a commutative ring with
  $\overline{1} \neq \overline{0}$, so it remains to produce an inverse for each
  non-zero element.

  Fix $\overline{h} \in A$ with $\overline{h} \neq \overline{0}$, and define
  \[
    T \colon A \longrightarrow A, \qquad
    T(\overline{g}) = \overline{h}\,\overline{g} .
  \]
  This is $F$-linear: multiplication in $A$ is distributive and $F$ sits in $A$
  as the classes of the constants, so
  $T(\overline{g_1} + \overline{g_2}) = T(\overline{g_1}) + T(\overline{g_2})$
  and $T(c\overline{g}) = cT(\overline{g})$ for $c \in F$.

  \textbf{$T$ is injective.} Suppose
  $\overline{h}\,\overline{g_1} = \overline{h}\,\overline{g_2}$. Subtracting,
  \[
    \overline{h}\,(\overline{g_1} - \overline{g_2}) = \overline{0} .
  \]
  Since $A$ is an integral domain and $\overline{h} \neq \overline{0}$, this
  forces $\overline{g_1} - \overline{g_2} = \overline{0}$, that is
  $\overline{g_1} = \overline{g_2}$.

  \textbf{$T$ is surjective.} By 1(a), $\dim_F A = n < \infty$, and $T$ is an
  $F$-linear map from $A$ to itself. An $F$-linear endomorphism of a
  finite-dimensional $F$-vector space is injective if and only if it is
  surjective, so $T$ is surjective.

  \textbf{The inverse.} Apply surjectivity to $\overline{1} \in A$: there is
  $\overline{g} \in A$ with
  \[
    T(\overline{g}) = \overline{h}\,\overline{g} = \overline{1} ,
  \]
  and injectivity makes this $\overline{g}$ unique. So $\overline{g} =
  \overline{h}^{\,-1}$, and every non-zero element of $A$ is invertible.

  Hence $A$ is a field.
\end{solution}
% ===== END SOLUTION 1(b) =====

\begin{problem}{3(a)}
  \emph{Degrees.} Let $K/F$ be a finite extension of degree $n$. Show that for
  any $\alpha \in K$, the degree of $m_{\alpha,F}$ divides $n$.
\end{problem}

% ===== SOLUTION 3(a) =====
% Transcribed from the student's slate, 2026-09-18 (page t0054-r51, card 0061),
% with the case they worked first on t0054-r50 (card 0060) as the special case.
% Same three steps, same order, nothing added to the route.
% One reason-correction, made here rather than left to the student: the page
% says [F(alpha):F] "must divide [K:F] by the tower law". The tower law gives an
% equation, not a divisibility; the quotient is [K:F(alpha)], which is a
% positive integer. The write-up states the equation and names the quotient.
% The page also uses m_{alpha,F} without saying why it exists, so the existence
% line from the finiteness of [K:F] is stated first.
\begin{solution}
  Let $[K:F] = n$ and let $\alpha \in K$.

  Since $\degree{K}{F} = n$ is finite, the $n+1$ elements
  $1, \alpha, \dots, \alpha^{n}$ cannot be linearly independent over $F$, so
  $\alpha$ is algebraic over $F$ and $m_{\alpha,F}$ exists.

  Consider $\degree{F(\alpha)}{F}$. Since $\alpha \in K$ and $F \subseteq K$, we
  have $F \subseteq F(\alpha) \subseteq K$, so the tower law gives
  \[
    n = \degree{K}{F}
      = \degree{K}{F(\alpha)}\,\degree{F(\alpha)}{F} .
  \]
  Here $\degree{K}{F(\alpha)}$ is a positive integer, so it is a witness $q$
  with $n = q \cdot \degree{F(\alpha)}{F}$; that is,
  $\degree{F(\alpha)}{F}$ divides $n$.

  Finally $\deg m_{\alpha,F} = \degree{F(\alpha)}{F}$, so $\deg m_{\alpha,F}$
  divides $n$.

  \medskip
  \textbf{The special case first worked.} With $F = \mathbb{Q}$,
  $K = \mathbb{Q}(\sqrt[4]{2})$ and $\alpha = \sqrt{2}$, take
  $M = \mathbb{Q}(\sqrt{2})$: then $\degree{M}{\mathbb{Q}} = 2$ divides
  $4 = \degree{\mathbb{Q}(\sqrt[4]{2})}{\mathbb{Q}}$. Nothing in that argument
  used $\sqrt[4]{2}$ or the number $4$, which is why $F(\alpha)$ in place of
  $M$ runs in general.
\end{solution}
% ===== END SOLUTION 3(a) =====

\begin{problem}{3(b)}
  Let $K/F$ be a finite extension of degree $n$. Show that if $n$ is prime,
  there are no fields strictly between $F$ and $K$.
\end{problem}

% ===== SOLUTION 3(b) =====
% Not re-posed, and not owed: this part is Garling's exercise 4.6, which the
% student proved on 2026-09-15. The region carries the pointer rather than a
% copy, so the argument has one home and cannot drift between two.
\begin{solution}
  This is Garling's exercise 4.6. It was proved on 2026-09-15 and is written up
  under Problem 04.6 in
  \texttt{chapters/ch04-field-extensions/homework/ch04-homework.tex}.
\end{solution}
% ===== END SOLUTION 3(b) =====

\begin{problem}{3(c)}
  Show that $\sqrt{2} \notin \mathbb{Q}(\sqrt[3]{2})$.
\end{problem}

% ===== SOLUTION 3(c) =====
% Transcribed from the student's slate, 2026-09-19 (page t0054-r54, card 0064).
% Same three steps in the same order: the degree of the minimal polynomial of
% sqrt(2), the degree of the extension, then 3(a) and 2 does not divide 3.
% Two reason-corrections, made here rather than left to the student:
%   - the page writes the minimal polynomial over the TOP field,
%     m_{alpha, Q(cbrt 2)}. 3(a) takes it over the bottom field, so the
%     write-up says m_{alpha, Q}. Over the top field every degree is 1 and the
%     divisibility is vacuous.
%   - the page gives deg m_{sqrt2,Q} = 2 from "the polynomial is x^2-2". Monic
%     with sqrt(2) as a root only bounds the degree above by 2; degree 1 is
%     ruled out by the irrationality of sqrt(2). That was agreed on card 0063
%     and is stated here.
\begin{solution}
  First, $\deg m_{\sqrt{2},\mathbb{Q}} = 2$. The polynomial is $x^{2} - 2$: it
  is monic, has $\sqrt{2}$ as a root and lies in $\mathbb{Q}[x]$, so
  $\deg m_{\sqrt{2},\mathbb{Q}} \leq 2$. Degree $1$ would make $\sqrt{2}$ a root
  of some $x - c$ with $c \in \mathbb{Q}$, that is $\sqrt{2} \in \mathbb{Q}$,
  and $\sqrt{2}$ is irrational. So the degree is exactly $2$.

  Further, $\degree{\mathbb{Q}(\sqrt[3]{2})}{\mathbb{Q}} = 3$, since
  $x^{3} - 2$ is irreducible over $\mathbb{Q}$.

  So by 3(a), applied to $K = \mathbb{Q}(\sqrt[3]{2})$ and $F = \mathbb{Q}$ with
  $n = 3$: for every $\alpha \in \mathbb{Q}(\sqrt[3]{2})$,
  $\deg m_{\alpha,\mathbb{Q}}$ divides $3$.

  So $\sqrt{2}$ cannot be in $\mathbb{Q}(\sqrt[3]{2})$, because
  $2 \nmid 3$.
\end{solution}
% ===== END SOLUTION 3(c) =====

\begin{problem}{6(a)}
  Let $K = \mathbb{Q}(\sqrt{2},\sqrt{3})$ and $\gamma = \sqrt{2} + \sqrt{3}$.
  Show that $\degree{K}{\mathbb{Q}} = 4$.
\end{problem}

% ===== SOLUTION 6(a) =====
% Transcribed from the student's slate, 2026-09-18 (page t0054-r42, turns
% leading to cards 0047-0050). Typeset exactly as written: same steps, same
% order, nothing added.
% Two reason-corrections, both agreed on cards 0048 and 0049 and made here
% rather than left to the student:
%   - the page justifies ab = 0 with "since a,b in Q"; the reason is that
%     sqrt(2) is irrational.
%   - the page flags each branch with "since b in Q" / "since a in Q"; the
%     contradictions are the irrationality of sqrt(6) and of sqrt(3).
\begin{solution}
  First, $\sqrt{3} \notin \mathbb{Q}(\sqrt{2})$.

  Suppose $\sqrt{3} = a + b\sqrt{2}$ with $a, b \in \mathbb{Q}$. Squaring,
  \[
    3 = a^{2} + 2ab\sqrt{2} + 2b^{2},
  \]
  so $2ab\sqrt{2} = 3 - a^{2} - 2b^{2}$ is rational, and since $\sqrt{2}$ is
  irrational this forces $ab = 0$.

  \emph{Suppose $a = 0$.} Then $\sqrt{3} = b\sqrt{2}$, so $2b = \sqrt{6}$. With
  $b \in \mathbb{Q}$ that makes $\sqrt{6}$ rational, and it is not.

  \emph{Suppose $b = 0$.} Then $a = \sqrt{3}$. With $a \in \mathbb{Q}$ that
  makes $\sqrt{3}$ rational, and it is not.

  So $\sqrt{3} \neq a + b\sqrt{2}$ for any $a, b \in \mathbb{Q}$, that is,
  $\sqrt{3} \notin \mathbb{Q}(\sqrt{2})$.

  \medskip
  \textbf{Note.} $\degree{\mathbb{Q}(\sqrt{2})}{\mathbb{Q}} = 2$, since
  $\mathbb{Q}(\sqrt{2}) = \mathbb{Q}[\sqrt{2}]$ has basis $\set{1, \sqrt{2}}$
  over $\mathbb{Q}$.

  \medskip
  \textbf{Further.} $\degree{K}{\mathbb{Q}(\sqrt{2})} = 2$, since
  $K = \mathbb{Q}(\sqrt{2})[\sqrt{3}]$ has basis $\set{1, \sqrt{3}}$ over
  $\mathbb{Q}(\sqrt{2})$: the minimal polynomial of $\sqrt{3}$ there divides
  $x^{2} - 3$, so has degree $2$ or $1$, and degree $1$ is ruled out by
  $\sqrt{3} \notin \mathbb{Q}(\sqrt{2})$.

  \medskip
  So by the tower law
  \[
    \degree{K}{\mathbb{Q}}
      = \degree{K}{\mathbb{Q}(\sqrt{2})}\,
        \degree{\mathbb{Q}(\sqrt{2})}{\mathbb{Q}}
      = 2 \cdot 2 = 4 .
  \]
\end{solution}
% ===== END SOLUTION 6(a) =====

\begin{problem}{6(b)}
  With $K$ and $\gamma$ as above, show that $K = \mathbb{Q}(\gamma)$, and
  compute $m_{\gamma,\mathbb{Q}}$.
\end{problem}

% ===== SOLUTION 6(b) =====
% Transcribed from the student's slate, 2026-09-18 (pages t0054-r43, t0054-r44,
% t0054-r45 and t0054-r46, cards 0051-0054). Typeset exactly as written: same
% steps, same order, nothing added. Both halves are now agreed.
% One reason-correction, agreed on cards 0051 and 0053 and made here rather than
% left to the student: r43 justifies minimality with "degree 4, monic, so this
% is the minimal polynomial". Monic of degree 4 is not enough on its own. The
% reason is the degree count the student then gave on r46, and that is what the
% write-up says.
\begin{solution}
  $(\sqrt{2}+\sqrt{3})(\sqrt{3}-\sqrt{2}) = 3 - 2 = 1$, so
  $\sqrt{3}-\sqrt{2} = \gamma^{-1}$, and hence
  $\sqrt{3}-\sqrt{2} \in \mathbb{Q}(\gamma)$.

  Take $\gamma + \gamma^{-1} = 2\sqrt{3} \in \mathbb{Q}(\gamma)$. Since
  $2 \in \mathbb{Q}$, that puts $\sqrt{3} \in \mathbb{Q}(\gamma)$. So
  $\gamma - \sqrt{3} = \sqrt{2} \in \mathbb{Q}(\gamma)$.

  So $\mathbb{Q}(\gamma) = \mathbb{Q}(\sqrt{2},\sqrt{3})$. Both contain
  $\sqrt{2}+\sqrt{3}$, $\sqrt{2}$ and $\sqrt{3}$; $\mathbb{Q}(\gamma)$ is the
  smallest field extension of $\mathbb{Q}$ containing $\sqrt{2}+\sqrt{3}$, and
  $\mathbb{Q}(\sqrt{2},\sqrt{3})$ is the smallest field extension of
  $\mathbb{Q}$ containing $\sqrt{2}$ and $\sqrt{3}$. So they must be the same.

  \medskip
  \textbf{The minimal polynomial.} Squaring twice,
  \[
    \gamma^{2} = 5 + 2\sqrt{6}
    \implies (\gamma^{2}-5)^{2} = 24
    \implies \gamma^{4} - 10\gamma^{2} + 25 = 24
    \implies \gamma^{4} - 10\gamma^{2} + 1 = 0 .
  \]

  Now $\degree{\mathbb{Q}(\gamma)}{\mathbb{Q}} = 4$, since
  $\mathbb{Q}(\gamma) = \mathbb{Q}(\sqrt{2},\sqrt{3})$ and that degree is $4$ by
  (a). So $\deg m_{\gamma,\mathbb{Q}} = 4$, meaning $x^{4} - 10x^{2} + 1$ is the
  monic polynomial which is $m_{\gamma,\mathbb{Q}}$: any lower degree cannot have
  $\gamma$ as a root.
\end{solution}
% ===== END SOLUTION 6(b) =====

\begin{problem}{6(c)}
  With $K$ and $\gamma$ as above, find three proper intermediate fields.
\end{problem}

% ===== SOLUTION 6(c) =====
% Transcribed from the student's slate, 2026-09-18 (page t0054-r47, card 0055).
% Typeset exactly as written: the three fields and, beside each, the elements of
% K it omits. Nothing added beyond naming what the page marks with arrows.
\begin{solution}
  $\degree{\mathbb{Q}(\gamma)}{\mathbb{Q}} = 4$, since
  $\mathbb{Q}(\gamma) = \mathbb{Q}(\sqrt{2},\sqrt{3})$.

  Intermediate fields of $\mathbb{Q}(\sqrt{2},\sqrt{3}) : \mathbb{Q}$:
  \[
    \begin{array}{lcl}
      \mathbb{Q}(\sqrt{2}) & \longrightarrow & \text{no } \sqrt{3},\ \sqrt{6}\\
      \mathbb{Q}(\sqrt{3}) & \longrightarrow & \text{no } \sqrt{2},\ \sqrt{6}\\
      \mathbb{Q}(\sqrt{6}) & \longrightarrow & \text{no } \sqrt{2},\ \sqrt{3}
    \end{array}
  \]

  Each is a proper intermediate field: each contains $\mathbb{Q}$ and sits
  inside $K$, each is bigger than $\mathbb{Q}$ because its generator is
  irrational, and none is all of $K$ because each omits the two elements of $K$
  named beside it. Those omissions also make the three fields distinct from one
  another.
\end{solution}
% ===== END SOLUTION 6(c) =====

\end{document}
