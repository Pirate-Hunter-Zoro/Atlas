<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Chapter 4 is finished.** 4.1, 4.3-4.11 closed and written up in
ch04-homework.tex, 4 pages, every line theirs. 4.2 is out -- they asked for it
to stay skipped. Do not re-teach any of it, and do not chase the 04.5 EMPTY
flag: archived t0050-r15 is their counterexample and it is already in the tex
as the remark.

**The worksheet, "Field Extensions and the Ring F[x]".** Its two pages are
live/inbox/uploads/20260915-1515*.png -- open those for a statement rather than
hunting; nothing else in the repo carries them. Order: 6, then 3(a) and 3(c),
then 1, 5, 2, 4. Part 3(b) is exercise 4.6, already proved, not re-posed.

**Problem 6 is closed, all three parts,** transcribed into
worksheet-field-extensions.tex with r42-r47 filed, builds clean at 2 pages.

**Solid, do not explain again:** degree as vector-space dimension, the tower
law, deg m = [F(alpha):F], minimal polynomials, bases, countability of K[x],
transcendence as licence to compare coefficients, the matrix of multiplication
by alpha and its characteristic polynomial, root 3 not in Q(root 2), and the
three intermediate fields of Q(root2, root3).

**The one live thing: sheet Problem 3(a),** posed whole on card 0055 -- K/F
finite of degree n, show deg m_{alpha,F} divides n. Posed with no sub-question
because both ingredients are already theirs. If they stall, the missing object
is F(alpha): the proof needs a middle field and they do not reach for one
unprompted.

**How this student works.** Never stuck on a fact, only on assembly. Their
error is always a wrong *reason* under a right answer -- correct it in the
write-up yourself and say so. A revision is appended, never recopied, so read
the LAST line and the side bubbles first; a phrase like "don't know how to
proceed" names a missing object, not a missing step.
