<!-- chapter: Ch 03 — Rings -->
**Ch 03, Rings (Garling).** Assigned: 3.1, 3.2, 3.3, 3.8, 3.10, 3.11, 3.12,
3.14, 3.15, 3.17, 3.25, 3.26.

**Tooling.** `scripts/build.sh` fails silently — use `board hw build`. Statements
are in `chapters/ch03-rings/reading/ch03.txt` under the section-end **Exercises**
headings. File pages to `chapters/ch03-rings/homework/handwritten/`. **Open the
PNGs.** Never ask this student to type mathematics.

## Where it got to

**7 of 12 written up:** 3.1, 3.3, 3.8, 3.10, 3.11, 3.12, 3.14. Builds clean.
Owed: **3.2** (skipped twice — *not now*, not never; bring it back unremarked
once the sheet is clear; they stalled clearing denominators for arbitrary α).
**3.8** infinite-K monomorphism half. **3.15, 3.17, 3.25, 3.26.**

**3.15 is the live thread.** ⇐ done and correct. ⇒ not. They wrote "K is a field
so (x−a)⁻¹ exists" — field-ness of K transported to K[x], and the argument never
used f(a)=0, so it proved every f lies in (x−a). They dropped that line
unprompted. Second try was contradiction, with the right question attached: *if
we can't pull an (x−a) from f, there's no way f(a) could be 0?* True — and the
contrapositive of the claim itself, so nothing to bite on.

## Right — do not re-teach

3.12 whole. 3.3 whole. 3.14 both directions. 3.15 ⇐. deg(fg)=deg f+deg g. Units
of R[x] and ℤ₄[x]. a ∈ (c) ⟺ c | a; (c) = R ⟺ c a unit.

## Next

**Card 0115 is open.** They have f = q(x−a) + r from Theorem 3.7 and are asked
what r can be given r = 0 or deg r < 1. Wanted: a constant. Then evaluate at a:
r = f(a) = 0. That closes 3.15; write it up.

## This student

**The architecture is sound; the last clause is not** — they prove the
interesting conjunct and drop the side condition. Name the missing one, singly.
They mis-enumerate m² + 5n² lists; check those yourself. Concrete lands; general
lands off two instances that disagree. **One** ask per card. They revise in
place, over earlier work, and often fix the wrong end — say which end. **Pencilled
questions get answered first, in their own card.**
