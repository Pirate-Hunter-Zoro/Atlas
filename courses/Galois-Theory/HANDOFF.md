<!-- chapter: Ch 03 — Rings -->
**Ch 03, Rings (Garling).** Assigned: 3.1, 3.2, 3.3, 3.8, 3.10, 3.11, 3.12,
3.14, 3.15, 3.17, 3.25, 3.26.

**Tooling.** `scripts/build.sh` fails silently — use `board hw build`. Statements
are in `chapters/ch03-rings/reading/ch03.txt` under the section-end **Exercises**
headings. File pages to `chapters/ch03-rings/homework/handwritten/`. **Open the
PNGs.** Never ask this student to type mathematics.

## Where it got to

**8 of 12 written up:** 3.1, 3.3, 3.8, 3.10, 3.11, 3.12, 3.14, **3.15**. Builds
clean.

Owed: **3.2** (skipped twice — *not now*, not never; bring it back unremarked
once the sheet is clear; they stalled clearing denominators for arbitrary α).
**3.8** infinite-K monomorphism half. **3.25, 3.26.**

**3.17 is live and card 0119 is open.** R = rationals writable as r/s with
p ∤ s; show it is a subring of ℚ, find its units, show it is a PID. Statement is
transcribed into the .tex already. The card asks only whether 5/2, 2/9, 6/3 lie
in R for p = 3. 6/3 is the point: it equals 2/1, so it belongs. Membership is
*some* representation, not every one — the ∃-not-∀ error from card 0086. Then
subring closure one axiom per card, then units, then PID.

## Right — do not re-teach

**3.15 whole**, both halves: factor at a root, deg g = n−1, a root b ≠ a forces
g(b) = 0 since no zero divisors, base case f = k₀ has no roots. 3.12 whole. 3.3
whole. 3.14 both directions. deg(fg) = deg f + deg g. Units of R[x] and ℤ₄[x].
a ∈ (c) ⟺ c | a; (c) = R ⟺ c a unit.

## This student

**The architecture is sound; the last clause is not** — they prove the
interesting conjunct and drop the side condition. Name the missing one, singly.
They mis-enumerate lists; check those yourself. Concrete lands; general lands
off two instances that disagree. **One** ask per card. They revise in place and
often fix the wrong end — say which end. Symbol slips while their prose is right
are pen: repair in the write-up, no redo. **Pencilled questions get answered
first, in their own card.**
