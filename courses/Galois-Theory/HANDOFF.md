<!-- chapter: New direction — We need to do all the exercises in chapter 4 -->
The run is all eleven exercises of chapter 4. Order: 4.1, 4.4, 4.6, 4.5, then
4.2, 4.7-4.11, with 4.3 last. 4.1, 4.4 and 4.6 are closed.

Closed, do not re-teach. 4.1: tower law, the full degree-7 pair list, and the
answer that the only intermediate fields are K and L. 4.4: they proved
x^3+3x+1 irreducible over Q unaided and produced both alpha^{-1} and
(1+alpha)^{-1} as rational combinations of 1, alpha, alpha^2. 4.6: their own
correct proof via 4.1. On 4.5 they now have both degrees right --
[L:K] = 3 by Eisenstein, [L(alpha):L] = 2 by the tower law -- and they found a
degree-2 polynomial over L killing 2^{1/6}.

What went wrong, twice, and both are the same shape: they hold a definition
loosely and do not check their answer against a number they just derived. First
they gave x^6 - 2 as the minimal polynomial over L, degree 6, contradicting the
2 on the same page -- "minimal polynomial of alpha" still defaults to over-Q for
them. Then they gave 2^{1/3}x^2 - 2^{2/3}, right roots, right degree, not monic.
Monic is not decoration; the minimal polynomial is x^2 - 2^{1/3}.

Where 4.5 stands. Card 0016 told them 4.5 AS PRINTED IS FALSE and that their own
tower is the counterexample, repaired the hypothesis to
gcd([K(alpha):K], [L:K]) = 1, restated the repaired exercise in full, and asked
one step: m_K is a polynomial over L with alpha as a root, so why does m_L
divide m_K, and what does that give for d vs n. That question is open.

Next after they answer it: the tower half. [L(alpha):K] equals both
d[L:K] and [L(alpha):K(alpha)]n, so n divides d[L:K]; coprimality gives n
divides d; with d <= n that forces d = n and m_L = m_K. Then 4.2.

How this student works. Algebra is reliable, the gaps are definitional and
structural. They skip warm-ups they judge beneath the exercise, so run those
inside the card. They copy the exercise at the top of the page; that is not a
question. Supply missing justifying lines yourself rather than re-posing them.
