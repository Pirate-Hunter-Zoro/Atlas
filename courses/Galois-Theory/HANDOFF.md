<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Closed in Chapter 4, never re-teach:** 4.1, 4.4, 4.5, 4.6, 4.8, 4.9. **4.2 is
out** -- they skipped it and asked for it to stay skipped. 4.8 and 4.9 are
typeset in ch04-homework.tex and it compiles clean; 4.8's page is filed.

**4.10 is one step from done, and every step of it is theirs.** Skeleton, the
split on gamma, the algebraic branch, and the transcendental branch's polynomial
argument -- alpha = f(gamma)/g(gamma), cleared, alpha*g(x) - f(x) graded over
K(alpha), non-zero because alpha is not in K, so gamma is algebraic over
K(alpha). Do not re-derive any of it.

**Degree is now solid.** It stalled two turns on degree as a vector-space
dimension. They first read [Q(sqrt2):Q] = 2 as the degree's default value rather
than as deg(x^2 - 2); that is fixed. They then ran x^3 - 2 correctly: degree 3,
basis {1, beta, beta^2}. They asked why the minimal polynomial's degree is the
number of basis elements, were given division with remainder plus minimality,
and reduced beta^4 to 2*beta in one trade. Answered and demonstrated. Do not
explain it again.

**Next, and start here.** Card 0026 poses the closing step of 4.10: the tower
K inside K(alpha) inside K(alpha, gamma), both degrees finite, so gamma is
algebraic over K, contradicting gamma transcendental. The tower law and "finite
degree implies every element is algebraic" are both new to them and are listed
on that card. Grade their answer, then typeset 4.10 in the same turn, file their
page and build. Then 4.7, 4.11, 4.3, then the worksheet "Field Extensions and
the Ring F[x]", Problem 6 half-open.

**How this student works.** They stop mid-drill and ask the real conceptual
question instead of pushing a wrong line -- answer it and re-pose the drill in
one card. They recopy the whole page each revision, so old work and old margin
questions reappear; only the bottom of the image is new. Their handwritten alpha
and beta are nearly identical.
