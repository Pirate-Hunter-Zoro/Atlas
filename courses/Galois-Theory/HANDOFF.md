# HANDOFF

2026-08-28, ~18:50. Garling §1.1, lecture. Chosen set: **1.3** (done), then **1.7**, then **1.5**.

## Where they got to

**1.3 (index 2 ⟹ normal) is finished, transcribed and compiled.** Both cases theirs:
$g \notin H$ — $gH$ is the other, disjoint coset; take $h \in H$, $x = ghg^{-1}$; if $x \in gH$
then $ghg^{-1} = gh_1$, cancel to $g^{-1} = h^{-1}h_1$, so $g \in H$, contradiction; hence
$x \notin gH$, so $x \in H$ by the partition. And $g \in H$ — closure, normality not needed.

In `ch01-homework.tex` as problem **01.3** (*"If $gHg^{-1}=H$, we're done"* omitted as they
asked). Build ok, 1 page, 0 warnings. Ink filed at `homework/handwritten/ch01-01.3.png`.

## Do not re-teach

Anything in 1.3: the coset partition at index 2, $gH = H$ iff $g \in H$, the cancellation, both
conjugation forms being one condition, closure covering $g \in H$.

## What went wrong was mine

Twice this session they stopped at a contradiction without discharging the supposition — fixed,
and gone. The real error was mine: **three cards (0010–0012) demanding they write
$gHg^{-1} \subseteq H$ under a proof that already fixed an arbitrary $h$ and never used which one
it was.** That is polish, not a gap. They pushed back and were right; card 0012 was withdrawn on
the board. When a proof is valid but not maximally explicit, say so once and move on.

## Next: exercise 1.7

Open with it directly — they have been told 1.3 is done and 1.7 is next, so no recap. It earns
its place by forcing element order and Lagrange together. One clause on the way in: card 0001's
fact list uses right cosets $H \circ g$ while every later card uses $gH$; say it once, do not
make a lesson of it. Then 1.5.

## How this student works

They patch the exact character you name and nothing adjacent, so a repeated request never lands —
change the form of the challenge. They will silently rewrite a cluttered page, so stale ink is not
current: read the bottom block, check stroke counts in `live/turns.jsonl`. They write plain-English
questions and instructions on the page; answer those first, never as `wrong`. When they say a step
is obvious, check — twice it was.

Board `http://127.0.0.1:8787/`; iPad `https://board.tail0c6c62.ts.net/`. `board recap` = the
lesson. No `$…$` in card titles. Save-and-push offer was left open on the board, unpushed.
