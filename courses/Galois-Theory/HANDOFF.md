<!-- chapter: Ch 03 — Rings -->
**Ch 03, Rings (Garling).** Assigned: 3.1, 3.2, 3.3, 3.8, 3.10, 3.11, 3.12,
3.14, 3.15, 3.17, 3.25, 3.26.

**Tooling.** `scripts/build.sh` fails silently — use `board hw build`.
Statements are in `chapters/ch03-rings/reading/ch03.txt` under the section-end
**Exercises** headings. File pages to
`chapters/ch03-rings/homework/handwritten/`. **Open the PNGs.** Never ask this
student to type mathematics.

## Where it got to

**11 of 12 written up:** 3.1, 3.3, 3.8, 3.10, 3.11, 3.12, 3.14, 3.15, 3.17,
3.25, and **3.26 part 1**. Builds clean, 8 pages.

Owed: **3.2** (skipped twice — *not now*, not never; bring it back unremarked
once the sheet is clear; they stalled clearing denominators for arbitrary α).
**3.8** infinite-K monomorphism half, granted.

**3.26 part 2 is live and card 0160 is open.** Show J[x] is not maximal in
R[x]. The card works concretely in ℤ[x] with J = (3) and asks only which of
x, 3x+1 generates all of ℤ[x] when thrown in with J[x]. Answer: 3x+1, because
(3x+1) − 3x = 1. Then generalise to I = J[x] + xR[x], and close it in three
asks: I is an ideal, J[x] ⊊ I (x ∈ I, x ∉ J[x] since 1 ∉ J), I ⊊ R[x]
(1 ∉ I, constant term).

## Right — do not re-teach

**3.26 part 1 whole:** least indices i, j; the coefficient of x^(i+j); a_i b_j
∉ J by primality; the dichotomy ℓ<i or m<j on every other pair; absorption;
the subtraction; contraposition and the or-to-and negation. **3.17, 3.25,
3.15, 3.14, 3.12, 3.3 whole.** Absorption, (c) = R ⟺ c a unit, a ∈ (c) ⟺ c | a.

## This student

**They prove the interesting conjunct and drop the side condition** — properness
went unwritten twice this session. Name the missing one, singly. Concrete
lands; general lands off two instances that disagree, so build the ask that
way. They mis-enumerate lists; check those yourself. One ask per card. They
revise in place and fix the wrong end — say which end. Symbol slips with
correct prose are pen: repair in the write-up, no redo. **Pencilled questions
get answered first, in their own card.**
