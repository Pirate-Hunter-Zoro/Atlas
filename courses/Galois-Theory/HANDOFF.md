<!-- chapter: Ch 03 — Rings -->
# HANDOFF

**Ch 03, Rings (Garling) — homework.** Assigned: **3.1, 3.2, 3.3, 3.8, 3.10,
3.11, 3.12, 3.14, 3.15, 3.17, 3.25, 3.26.** Sheet:
`handwritten/20260902-exercise-list.png`.

**Tooling.** `scripts/build.sh` fails silently on the Mac — use `board hw
build`. File pages to `chapters/ch03-rings/handwritten/`.

## Where it got to

**3.1, 3.10** complete and transcribed. **3.8** all but the infinite case.
**3.11** all but one clause. **3.2, 3.3** skipped 2026-09-02 ("I have lost
patience") — still owed, come back unremarked.

## Right — do not re-teach

deg(fg) = deg f + deg g over a domain, via aₙbₘ ≠ 0, proved unaided. Units of
R[x] = units of R, as constants. Units of ℤ₄[x]: constant term a unit, other
coefficients zero divisors; 2x+3 found unaided. b + (a) = 0 ⟺ a | b. Both the
forward direction of 3.11 and the divisibility half of its converse.

## Next

**Finish 3.11's last clause: R/(a) a domain ⟹ a is not a unit.** Everything
else in the problem is transcribed; this one line closes it. They are genuinely
stuck on why (a) = R when a is a unit — card 0079 asks for (2) in ℚ, aimed at
their reading of it as "the even ones". Then s = s(ra) = (sr)a gives (a) = R,
one class, 1 = 0. Then 3.12.

Garling (p. 362) defines prime only for **non-zero** a, and 3.11 needs that:
R/(0) ≅ R is a domain while 0 is not prime.

## This student

Concrete lands; general lands straight off **two instances that disagree** —
one is never enough, and they will promote a single instance to a theorem (ℤ's
±1 became "the only units in any R[x]").

They revise one page in place, and when sent back they often **fix the wrong
end** — moved a slash off the conclusion, not the hypothesis. Say which end.

Pencilled questions get answered first, in their own card; their questions are
better than their attempts. **"I'm stuck" is unreliable when a page comes with
it** — read the working. With no page, it is real. Their handwritten | and ∤
are near-identical: check against the argument, not the ink.
