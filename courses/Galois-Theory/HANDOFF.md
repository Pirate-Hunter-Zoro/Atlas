<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Closed in Chapter 4, never re-teach:** 4.1, 4.4, 4.5, 4.6, 4.8, 4.9. **4.2 is
out** — they skipped it and asked for it to stay skipped. 4.8 and 4.9 are
typeset in ch04-homework.tex and it compiles clean; 4.8's page is filed.

**4.10 is in progress and the skeleton is theirs.** Suppose K(alpha,beta) =
K(gamma). Gamma algebraic gives beta algebraic — contradiction, branch closed,
do not re-open it. Gamma transcendental is the live branch. They also own the
supporting fact: gamma transcendental and deg f >= 1 makes f(gamma)
transcendental, since deg h(f(x)) = deg h * deg f >= 1.

**Where they stopped, and it is a good stop.** Writing alpha = f(gamma)/g(gamma),
they tried to substitute that quotient into h, the polynomial killing alpha,
then stopped themselves in the margin: "why is this in K[x]? I don't think it
is", and "where do we use alpha algebraic but not in K?" Both questions are
right. h(f(x)/g(x)) is a rational function in K(x), not a polynomial, and alpha
algebraic is not used at that step — only alpha not in K is. Card 0018 answers
both and redirects: multiply through by g(gamma) instead.

**Next.** Grade the polynomial alpha*g(x) - f(x), coefficients alpha*g_i - f_i
in K(alpha). Then why it is non-zero: if it were, alpha = f_i/g_i lies in K.
That is where alpha not in K is spent. Then gamma algebraic over K(alpha),
K(alpha):K finite as alpha is algebraic, tower law, gamma algebraic over K,
contradiction. Then typeset 4.10, then 4.7, 4.11, 4.3, then the worksheet
"Field Extensions and the Ring F[x]", Problem 6 half-open.

**How this student works.** They stop mid-drill and ask the real conceptual
question rather than pushing a wrong line — answer it and re-pose the drill in
the same card. They argue back when they are right, and have been right twice.
Their handwritten alpha and beta are nearly identical; read glyphs slowly
before correcting a line. Asking for the next question means give the whole
exercise.
