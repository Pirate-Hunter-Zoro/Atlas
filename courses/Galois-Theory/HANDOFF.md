<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Chapter 4 is finished.** 4.1, 4.3-4.11 closed and written up in
ch04-homework.tex. 4.2 is out -- they asked for it to stay skipped, and it is
the one thing `board hw status` still calls EMPTY in `ch04`. That flag is
expected; do not re-pose it, and do not re-teach any of the rest. 4.5 as printed
is false and their counterexample is now typeset in its own solution region, so
it no longer flags.

**The worksheet, "Field Extensions and the Ring F[x]".** Both pages are
live/inbox/uploads/20260915-1515*.png -- open those for a statement rather than
hunting; nothing else in the repo carries them. Remaining order: 3(c), then 1,
5, 2, 4. Part 3(b) is exercise 4.6, already proved, not re-posed.

**Closed and written up:** Problem 6, all three parts, and Problem 3(a).
homework/worksheet-field-extensions/worksheet-field-extensions.tex carries both
in sheet order, r42-r47 and r51 filed, builds clean at 2 pages. Its parts are
one `problem` environment each, matched opener and closer per part, because that
is what `board hw status` pairs by label -- fold 3(a) back into a statement of 3
and the board reports the whole of 6 as unwritten. Keep the shape when you add
3(c).

**Solid, do not explain again:** degree as vector-space dimension, the tower
law, deg m = [F(alpha):F], minimal polynomials, transcendence as licence to
compare coefficients, the matrix of multiplication by alpha and its
characteristic polynomial, root 3 not in Q(root 2), the three intermediate
fields of Q(root2, root3), and -- new this session -- reaching for a middle
field F(alpha) unprompted. That last took three cards to install; it is theirs.

**The one live thing: sheet Problem 3(c),** posed whole on card 0061 -- show
root 2 is not in Q(cbrt 2). No sub-question, because it is 3(a) applied once:
degree 3 on the bottom, degree 2 would have to divide it. Read whether they
justify deg m_{root2,Q} = 2 or just assert it.

**How this student works.** Never stuck on a fact, only on assembly. Their
error is always a wrong *reason* under a right answer -- on 3(a) they said the
tower law makes the degree divide, when it gives an equation and divisibility
needs the other factor named as a positive integer. Fix that in the write-up
yourself, say on the card that the file now says it right, and do not re-pose.
A revision is appended, never recopied: read the LAST line and the bubbles.
