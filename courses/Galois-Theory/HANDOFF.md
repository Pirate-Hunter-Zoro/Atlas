<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Closed in Chapter 4, never re-teach:** 4.1, 4.4, 4.5, 4.6, 4.8, 4.9, 4.10.
**4.2 is out** -- they skipped it and asked for it to stay skipped. 4.8, 4.9 and
4.10 are typeset in ch04-homework.tex, pages r20 and r21 filed, compiles clean
at 4 pages.

**4.10 is finished and every line is theirs.** The case split on gamma, the
algebraic branch through "beta would be algebraic", the non-vanishing of
f(x) - alpha*g(x) over K(alpha) by picking ONE index i with g_i non-zero, the
tower K inside K(alpha) inside K(alpha,gamma), the tower law, and the close from
finite degree to gamma algebraic over K. Do not re-derive any of it.

**Degree is solid.** Degree as vector-space dimension, [Q(sqrt2):Q] = 2 as
deg(x^2 - 2), basis {1, beta, beta^2} for x^3 - 2, beta^4 = 2*beta in one trade.
Demonstrated, not guessed. Do not explain it again.

**Start on 4.7,** posed in full on card 0028, nothing of it answered. They were
asked one piece: how big the set of degree-n polynomials over a countable K is,
then how big K[x] is, naming the counting fact at each step. It is next because
it needs no field theory -- counting only, a clean restart after five cards of
one proof. Then 4.11, 4.3, then the worksheet "Field Extensions and the Ring
F[x]", Problem 6 half-open.

**How this student works.** They write correct arguments that stop one step
short of the conclusion, so read the last line before grading. When the missing
step is one clause they have plainly understood, a tick at the end of the page
IS them claiming it -- do not ask a fourth time for a sentence. They stop
mid-drill to ask the real conceptual question rather than push a wrong line;
answer it and re-pose the drill in one card. They usually recopy the whole page
each revision, so only the bottom is new. Their alpha and beta look identical.
They say "algebraic in K" for "over K", still.
