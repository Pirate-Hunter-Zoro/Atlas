<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Chapter 4 is finished.** 4.1, 4.3-4.11 closed and written up in ch04-homework.tex.
4.2 is out by their request and is the one thing `board hw status` still calls EMPTY in
`ch04`. Expected; do not re-pose it, do not re-teach the rest.

**The worksheet, "Field Extensions and the Ring F[x]."** Both pages are
live/inbox/uploads/20260915-1515*.png -- open those for a statement. Closed and typeset
in worksheet-field-extensions.tex, in sheet order, building clean: Problem 6 (all three
parts), 3(a), 3(c). 3(b) is exercise 4.6, already proved. Each part is its own `problem`
environment with matched opener and closer, because that is what `board hw status` pairs
by label. Keep that shape. Order after 1: 5, 2, 4.

**The one live thing: Problem 1(a).** Show the classes of 1, x, ..., x^(n-1) are a basis
of A = F[x]/(f) over F, deg f = n. Spanning is half-done. They set it up and stopped:
take g, divide, g = qf + r. They have not barred both sides, so "bar g = bar r" is the
open question, posed on card 0069. Then independence off deg r < n, then 1(b), 1(c).

**Two errors, both about quotients.** A circled "x in F" -- x is the indeterminate, and
the root of f in A is its class. And division with remainder stated as deg r < deg f
alone, dropping r = 0. Corrected on cards 0068-0069; do not re-explain either.

**Solid, do not explain again:** degree as dimension, the tower law, deg m =
[F(alpha):F], minimal polynomials, transcendence as licence to compare coefficients,
multiplication-by-alpha and its characteristic polynomial, the intermediate fields of
Q(root2, root3).

**How this student works.** Never stuck on a fact, only on assembly; their error is a
wrong *reason* under a right answer. They stop mid-derivation and ask "am I right so
far?" -- answer that first, in its own card, then hand back the same step. Bars and
subscripts drop after line one. A revision is appended: read the last line and the bubbles.
