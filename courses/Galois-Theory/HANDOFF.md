<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Closed in Chapter 4, never re-teach:** 4.1, 4.4, 4.5, 4.6, 4.8, 4.9. **4.2 is
out** — they skipped it and asked for it to stay skipped. 4.8 and 4.9 are
typeset in ch04-homework.tex and it compiles clean; 4.8's page is filed.

**4.10 is nearly theirs.** The skeleton, both cases, and the polynomial argument
are their own work: gamma algebraic gives beta algebraic, contradiction, branch
closed. In the transcendental branch they wrote alpha = f(gamma)/g(gamma),
cleared the denominator, graded alpha*g(x) - f(x) over K(alpha), and got it
non-zero from alpha not in K. That yields gamma algebraic over K(alpha). Do not
re-derive any of it.

**Where it stalled, and it is not the algebra.** The last step is the tower law,
and "finite degree" meant nothing to them — degree as a vector-space dimension
had never been made concrete. Card 0023 defined it.

**The misunderstanding, exactly.** Shown [Q(sqrt2):Q] = 2 with basis {1, sqrt2},
they took 2 as the degree's default value rather than as the degree of x^2 - 2.
Asked for [Q(beta):Q] with beta a root of x^3 - 2, they got beta = cbrt(2) right
and then answered 2, basis {1, beta}, asking in the margin "will it always be 2?"
Card 0024 answers that, hands them deg(x^3 - 2) = 3, and re-poses the same case
for the degree and the basis. Their wrong 2 is deliberately unmarked. Start by
grading their re-answer.

**Next.** Once 3 and {1, beta, beta^2} land, finish 4.10: K(alpha):K is finite as
alpha is algebraic, gamma is algebraic over K(alpha), tower law, so gamma is
algebraic over K, contradiction. Then typeset 4.10, then 4.7, 4.11, 4.3, then the
worksheet "Field Extensions and the Ring F[x]", Problem 6 half-open.

**How this student works.** They stop mid-drill and ask the real conceptual
question instead of pushing a wrong line — answer it and re-pose the drill in one
card. They argue back when right, and have been. Their handwritten alpha and beta
are nearly identical; read glyphs slowly.
