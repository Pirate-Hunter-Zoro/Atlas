# HANDOFF

2026-08-28, ~19:00. Garling §1.1, lecture. **The exercise set changed this session.**

## The new list is the student's, not mine

At 18:57 they uploaded a page of problems (filed at
`chapters/ch01-groups-fields-and-vector-spaces/handwritten/ch01-problem-list.pdf`) with a note
across it: *"ALL THESE ARE QUESTIONS FOR US. I HAVE STARTED SOME BUT WE'LL GO FROM SCRATCH. SOME
ARE FROM BOOK AND SOME FROM DR. COX-STEIB."* Some are Garling's, some are their instructor's.
There is partial ink on three of them — **do not grade it**, they said from scratch.

The page also carries 1.6, 1.7, 1.9, 1.13, 1.14, 1.18 circled, and a chapter-3 block on principal
ideal domains ((2,6) = (2), (7,1726) = (gcd), ℤ[x]). Not this session's business.

## Chosen set, announced on card 0014, in this order

1. **H = all elements of G of order dividing n (n ≥ 2) is normal.** Engine: conjugation preserves
   order. They already had that computation on the page, so this is a fast bank.
2. **H the only subgroup of order n = |H| is normal.** Same engine on the whole subgroup: gHg⁻¹ is
   a subgroup, has n elements, so it *is* H.
3. **|G| even ⟹ some g ≠ e with g² = e.** Counting/pairing, not conjugation. Their margin note
   *"something is its own inverse"* is already the right idea.
4. **1.7** — G generated by each of its non-identity elements iff G is finite of prime order.

## Done, do not re-teach

**1.3 (index 2 ⟹ normal)** is finished, transcribed as problem 01.3 in `ch01-homework.tex`, and
compiled. It is the third problem on their new page — struck off on card 0014. Also settled: the
coset partition at index 2, gH = H iff g ∈ H, the cancellation, both conjugation forms being one
condition, closure covering the g ∈ H case.

## Open right now

Card 0014 ends with a hand-check, not the exercise: in S₃ with h = (1 2), g = (1 2 3), compute
ghg⁻¹ and give its order. Answer is (2 3), order 2. If they get it, go straight to teaching
exercise 1 above; if they miss it, the misunderstanding is conjugation itself and it gets its own
card.

## What went wrong last time was mine

Cards 0010–0012 pressed three times for polish (writing gHg⁻¹ ⊆ H under a proof that had already
fixed an arbitrary h). They pushed back and were right; 0012 was withdrawn. When a proof is valid
but not maximally explicit, say so once and move on.

## How this student works

They patch the exact character you name and nothing adjacent, so a repeated request never lands —
change the form of the challenge. They will silently rewrite a cluttered page, so stale ink is not
current: read the bottom block, check stroke counts in `live/turns.jsonl`. They write plain-English
questions and instructions on the page; answer those first, never as `wrong`. When they say a step
is obvious, check — twice it was. They also hand over pictures to steer the lesson; treat those as
instructions, not answers.

Board `http://127.0.0.1:8787/`; iPad `https://board.tail0c6c62.ts.net/`. `board recap` = the
lesson. No `$…$` in card titles. Save-and-push offer was left open on the board, unpushed.
