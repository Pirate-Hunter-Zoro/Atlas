<!-- chapter: New direction — Let’s prioritize the remaining book problems except the one… -->
PLAN.md at the root has the order of what is open; the README points at it.

**Closed in Chapter 4, never re-teach:** 4.1, 4.4, 4.5, 4.6, 4.8, 4.9. **4.2 is
out** -- they skipped it and asked for it to stay skipped. 4.8 and 4.9 are
typeset in ch04-homework.tex and it compiles clean; 4.8's page is filed.

**4.10 is one sentence from done, and every line of it is theirs.** Page r20
writes the whole thing out: the skeleton, the split on gamma, the algebraic
branch, and in the transcendental branch alpha = f(gamma)/g(gamma) cleared to
f(x) - alpha*g(x), non-zero over K(alpha) because alpha is not in K, so gamma
is algebraic over K(alpha). Then the tower K inside K(alpha) inside
K(alpha,gamma), both steps finite with correct reasons, tower law giving
[K(alpha,gamma):K] finite. Do not re-derive any of it.

**The one gap, and start here.** They stop at "is finite" and never use it.
Finiteness reads to them as the conclusion rather than as a premise. "Finite
implies algebraic" was listed on cards 0026 and 0027 and went unused both
times. Card 0027 grades everything above as right and asks only the closing
sentence: gamma is algebraic over K, contradicting gamma transcendental. Grade
that, then typeset 4.10, file r20 and build in the same turn.

**Degree is solid.** Degree as vector-space dimension, [Q(sqrt2):Q] = 2 as
deg(x^2 - 2), basis {1, beta, beta^2} for x^3 - 2, and beta^4 = 2*beta in one
trade. Answered and demonstrated. Do not explain it again.

After 4.10: 4.7, 4.11, 4.3, then the worksheet "Field Extensions and the Ring
F[x]", Problem 6 half-open.

**How this student works.** They stop mid-drill and ask the real conceptual
question instead of pushing a wrong line -- answer it and re-pose the drill in
one card. They usually recopy the whole page each revision, so only the bottom
is new; r20 was an exception and was entirely fresh. Their handwritten alpha
and beta are nearly identical. And they write correct arguments that stop one
step short of the conclusion, so read the last line before grading.
