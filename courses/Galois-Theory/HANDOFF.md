# HANDOFF

2026-08-28, ~19:00. Garling §1.1, lecture.

## The exercise set changed — it is theirs now

They uploaded a page of problems (filed at
`chapters/ch01-groups-fields-and-vector-spaces/handwritten/ch01-problem-list.pdf`), some Garling's
and some from Dr. Cox-Steib, with a note across it: *we'll go from scratch*. There is partial ink
on three of them — **do not grade it**. Circled for later: 1.6, 1.9, 1.13, 1.14, 1.18, and a
chapter-3 block on principal ideal domains.

## Done — do not re-teach

**Index 2 ⟹ normal** (their third problem, Garling 1.3): finished, transcribed as problem 01.3 in
`ch01-homework.tex`, compiled. Settled with it: the coset partition at index 2, gH = H iff g ∈ H,
the cancellation step, both conjugation forms being one condition, closure covering the g ∈ H case.

## The plan, announced on the board

1. H = all elements of order dividing n is normal — engine: conjugation preserves order.
2. H the only subgroup of order n is normal — same engine, whole subgroup: gHg⁻¹ is a subgroup of
   size n, hence is H.
3. |G| even ⟹ some g ≠ e with g² = e — pairing, not conjugation.
4. Garling 1.7 — generated by each non-identity element iff finite of prime order.

## Next

A hand-check is open and unanswered: in S₃ with h = (1 2), g = (1 2 3), compute ghg⁻¹ and its
order. Answer: (2 3), order 2. If it lands, teach exercise 1 above. If it misses, conjugation
itself is the gap and it gets its own card — everything after it depends on that operation.

## What went wrong was mine

I pressed three times for polish on a proof that was already valid (demanding gHg⁻¹ ⊆ H under an
argument that had fixed an arbitrary h). They pushed back and were right. Say it once, move on.

## How this student works

They patch the exact character you name and nothing adjacent — repeat a request and it never
lands; change its form. They silently rewrite a cluttered page, so read the bottom block, not the
stale ink. They write plain-English questions on the page: answer those first, never as `wrong`.
Pictures they hand over are instructions about what to do next, not answers. When they call a step
obvious, check — twice they were right.

Board `http://127.0.0.1:8787/`; iPad `https://board.tail0c6c62.ts.net/`. No `$…$` in card titles.
Unpushed.
