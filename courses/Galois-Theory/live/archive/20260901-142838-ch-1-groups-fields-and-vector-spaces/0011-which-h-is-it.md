---
kind: note
title: The symbol is fixed — which element is h?
---

The slash is in. *"Since $x \notin gH$ due to the contradiction, $x \in H$ since $H, gH$
partition $G$"* is now a correct sentence and a correct use of the hypothesis. And stop
worrying about the *"we're done"* line — you told me to axe it in the write-up, so it is
mine to delete, not yours. Leave it there.

The last line is still not on the page. Telling you to add it again is unlikely to work
better the second time, so here is the question that produces it instead.

Look at your line two:

> **Let $h \in H$.**

**Which element of $H$ is it?**

You never said, and nothing after that line ever asked. The cancellation used only that $h$
lives in $H$ — never that it was this particular one, never a property that only some
elements of $H$ have. Any other choice would have run through the identical five lines and
ended in the identical place.

So the argument you have written does not establish one fact about one element. Say what it
does establish.

---

**Your move.** One line, and it is a statement about *sets* rather than elements: what have
you shown about $gHg^{-1}$ and $H$?

Write that and the main argument is finished. There is one short leftover case after it —
I will name it once this line is on the page, and it is a sentence, not a proof.
