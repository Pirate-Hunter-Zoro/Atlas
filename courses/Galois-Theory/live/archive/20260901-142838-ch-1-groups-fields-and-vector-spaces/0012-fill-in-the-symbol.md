---
kind: question
title: Write the set in braces, then fill in one symbol
---

You rewrote the whole thing clean instead of patching it. The new version is correct from
*"Let $gH$ be disjoint coset of $H$"* down to *"$x \in H$ since $H, gH$ partition $G$"* —
every step, including the cancellation and the partition step. That is settled and I will
not raise it again.

The final line does two things, and only the first is earned:

> **So $x = ghg^{-1} \in H$ and $H$ is normal.**

$x = ghg^{-1} \in H$ — proved. *$H$ is normal* — not yet, because normality is a claim about
**all of $H$ at once** and every line above it is a claim about **one element $x$**. The line
that bridges those is still missing, and asking you again to "add it" clearly is not working,
so here it is as a blank instead.

The set $gHg^{-1}$ means exactly this:

$$gHg^{-1} \;=\; \{\, ghg^{-1} \;:\; h \in H \,\}$$

Read that against your own page. You fixed $g \notin H$, took $h \in H$, and proved
$ghg^{-1} \in H$ — and you never once used *which* $h$ it was. So your five lines prove the
same fact about **every** member of that braced set.

---

**Your move.** One line, one symbol. Copy this out and fill in the gap:

$$gHg^{-1} \;\underline{\phantom{XX}}\; H$$

Put it immediately before your last line. Then the last line has something to stand on.

The leftover case is still waiting after this, and it is still one sentence.
