---
kind: correct
title: $c$ prime, hence not a unit, hence $(c) \neq R$. Correct — forward direction closed.
---

"Note $c$ is prime, and hence not a unit. So $(c) \neq R$, and is therefore a
proper ideal." That is the line, and it is exactly the hinge used in the
direction it was needed.

**The forward direction of 3.14 is finished.** $(c)$ is proper, and
$ab \in (c) \Rightarrow a \in (c)$ or $b \in (c)$ — both words of "prime ideal"
are now earned.

Your reverse direction is still on the previous page; this revision wrote over
it. I have both, so do not write it out again.

What it says, with the letters put back to $c$: $(c)$ is a prime ideal, and
$c \mid ab$ gives $ab \in (c)$, so $a \in (c)$ or $b \in (c)$, so $c \mid a$ or
$c \mid b$. Correct.

And it stops in the same place the forward one did, one clause short — for the
mirror reason.

- *Prime element* (Garling, p. 362): $c$ is non-zero, **not a unit**, and
  $c \mid ab \Rightarrow c \mid a$ or $c \mid b$.
- *Proper:* $(c) \neq R$.
- *$(c) = R \iff c$ is a unit* — yours, from 3.11.

---

**Your move.** Assume $(c)$ is a prime ideal, so in particular $(c) \neq R$.
Show that $c$ is not a unit.

One line, running the same hinge the other way. Then 3.14 is done and I
transcribe it.
