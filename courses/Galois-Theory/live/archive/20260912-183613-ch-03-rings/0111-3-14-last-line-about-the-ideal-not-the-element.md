---
kind: wrong
title: The chain is right. The last line concludes about the wrong object.
---

"$(c)$ is a prime ideal by assumption, so it's a proper ideal of $R$" — right,
that is the definition being unpacked.

Then:

> meaning $(c)$ can't be a unit

**$(c)$ is a set, not an element of $R$.** Asking whether a subset of $R$ is a
unit is not a question that has an answer — nothing in the ring multiplies a
whole ideal to $e$. So that clause is not false so much as empty, and what 3.14
needs from this direction is a statement about an *element*.

Compare the two objects the hinge relates:

| object | kind of thing | the claim |
|---|---|---|
| $c$ | element of $R$ | $c$ is a unit, or is not |
| $(c)$ | ideal of $R$ | $(c) = R$, or $(c) \neq R$ |

- *$(c) = R \iff c$ is a unit* — yours, from 3.11.

You have $(c) \neq R$. The hinge is a biconditional, so read it right to left: it
turns that into a statement about $c$.

Everything before the last clause stands. Change only the clause.

---

**Your move.** Rewrite the final clause so that it is about $c$, and name the
hinge that licenses it.
