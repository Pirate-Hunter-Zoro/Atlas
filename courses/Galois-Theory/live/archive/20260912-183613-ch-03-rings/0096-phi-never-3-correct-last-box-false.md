---
kind: wrong
title: $\varphi$ never takes the value $3$ — correct. The last box is not needed, and is false.
---

Everything down to the boxed **NO** is a proof, and it is the right one.
$\varphi(2 + i\sqrt{5}) = 9$; $\varphi$ is multiplicative; the splittings of $9$
in $\mathbb{N}$ are $1 \cdot 9$, $3 \cdot 3$, $9 \cdot 1$; and $m^2 + 5n^2 = 3$
has no integer solutions, so $3 \cdot 3$ is gone. That was the only branch that
could have caused trouble.

Two branches remain, $1 \cdot 9$ and $9 \cdot 1$, and they are the same branch
with the letters swapped. So one of the two factors has $\varphi = 1$ — and your
next line says exactly what such an element is: $m = \pm 1$, $n = 0$. That line
is correct, and it is where the proof ends.

You did not write the end.

Instead you went on to the last box, and two things are wrong with it. Its
solution list is incomplete: $m^2 + 5n^2 = 9$ also has $m = \pm 3$, $n = 0$, so
$\beta = \pm 3$ is not excluded by $\varphi(\beta) = 9$. And you never needed
the list at all — nothing about the factor with $\varphi = 9$ has to be checked.
The whole contradiction comes from the other factor.

---

**Your move.** Take the branch $\varphi(\beta) = 1$, where you have already
shown $\beta = \pm 1$.

Write the two lines that finish it: what $\beta = \pm 1$ makes $\beta$, and what
you supposed about $\beta$ on the line beginning *Suppose*.
