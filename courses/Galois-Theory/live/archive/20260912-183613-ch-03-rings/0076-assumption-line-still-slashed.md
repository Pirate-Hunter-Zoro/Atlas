---
kind: wrong
title: You fixed the wrong end of it
---

The last line is right now — $bc + (a) = (a)$ **since $a \mid bc$**. Good.

But that is the line that was following correctly from the assumption. The
assumption itself is still what it was:

> $\exists\, b, c$ s.t. $a \nmid bc$, but $a \nmid b$ and $a \nmid c$

So the page now asserts $a \nmid bc$ at the top and uses $a \mid bc$ at the
bottom. Same two lines, same conflict — you moved the slash off the conclusion
instead of off the hypothesis.

---

**Your move.** Your own $\mathbb{Z}/(6)$ example is an instance of exactly this
assumption. Take
$$a = 6, \qquad b = 2, \qquad c = 3.$$

Write down the three divisibility facts that made $2 + (6)$ and $3 + (6)$ a
zero-divisor pair — one for $bc$, one for $b$, one for $c$ — each with a slash
or without, whichever is true of the actual numbers.
