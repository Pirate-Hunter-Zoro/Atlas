---
kind: note
title: That is the same page twice — the card above is its answer
---

Both sends carry identical ink, three seconds apart. Nothing new to read, so I
am not marking it a second time: **card 0039 above is the reply to it.**

The ask still standing, so it is at the bottom of the board where you are
looking rather than a card up:

- **$c_2$** — the pairs $(i,j)$ with $i+j=2$, $i \in \{0,1\}$, $j \in \{0,1,2\}$.
  There are two. You used one.
- **$c_3$** — same question for $i+j=3$. There is one, and it is not nothing.
- **The first line of your proof** — rewrite it with $c_m = \sum_{i+j=m} a_ib_j$
  in place of $\sum(a_ib_i)x^i$.
