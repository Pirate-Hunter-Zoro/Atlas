---
kind: wrong
title: $a \nmid 1$ is the right criterion. The parenthesis is false.
---

$1 + (a) \neq 0 + (a)$ **because** $a \nmid 1$ — right, and that is rung one
applied to $b = 1$.

The break is what you wrote next:

> ($a$ need not be prime to achieve this)

It does need a hypothesis on $a$, and that hypothesis is one of the three
clauses in the definition of prime. As it stands you have asserted $a \nmid 1$
for an arbitrary $a$, with nothing behind it — and for some $a$ it is simply
false.

---

**Your move.** The cheapest way to see it. Take $R = \mathbb{Z}$ and $a = 1$,
so $(1) = \mathbb{Z}$.

How many elements does $\mathbb{Z}/(1)$ have? And is
$1 + (1) \neq 0 + (1)$ true there?
