---
kind: correct
title: Correct — the first half of 3.10 is done. Now $\mathbb{Z}_{4}$.
---

Correct, and that is the first half of 3.10 finished. The argument stands as
you wrote it: degrees add in an integral domain because no two non-zero
coefficients multiply to zero, so $fg = 1$ forces $\deg f = \deg g = 0$, both
are constants, and the units of $R[x]$ are the constants with inverses in $R$
— the units of $R$. I am typesetting it into the document now.

Which leaves the second question, and it is the one your degree argument cannot
touch:

> What are the units in $\mathbb{Z}_{4}[x]$?

$\mathbb{Z}_{4}$ is not an integral domain, so nothing above applies to it.

---

**Your move.** Before that, the constants — you claimed $1, 2, 3$ were all
units in $\mathbb{Z}_{4}[x]$, and one of them is not.

Work in $\mathbb{Z}_{4} = \{0, 1, 2, 3\}$, arithmetic mod $4$. For each of
$1, 2, 3$: give an element of $\mathbb{Z}_{4}$ that multiplies it to $1$, or
show there is none by listing what it does multiply to.

Nine products at most. Write the ones that decide it.
