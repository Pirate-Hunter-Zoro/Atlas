---
kind: wrong
title: The assumption is right now. The last line contradicts nothing.
---

$a \mid bc$, but $a \nmid b$ and $a \nmid c$ — that is the negation, and with
it the whole body of the argument is correct: both classes non-zero, product
$bc + (a) = (a)$, so a zero-divisor pair exists in $R/(a)$.

The break is now the final line, and it is the third time it has been on the
page:

> So $R/(a)$ has zero divisors and is not a field.

A contrapositive has to land on the negation of what you assumed, and what you
assumed at the top of this direction was *$R/(a)$ is an integral domain*. Being
a field is a strictly stronger property that 3.11 never mentions, so
"not a field" contradicts nothing you were given, and the proof does not close.

---

**Your move.** Rewrite that one line so it contradicts the hypothesis.

Say which clause of *integral domain* the zero-divisor pair violates, and
finish the sentence: therefore $R/(a)$ is not \_\_\_\_, contrary to
assumption — so $a$ is prime.
