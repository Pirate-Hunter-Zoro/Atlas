---
kind: correct
title: Correct — the divisibility clause is closed. One clause left in 3.11.
---

Correct. Zero divisors contradict *integral domain*, which is what you assumed,
so the contrapositive closes and $a$ satisfies the divisibility clause of
prime. Typesetting it now.

That leaves one thing in the whole of 3.11 — the clause you were asked for two
pages back and have not come back to.

---

**Your move.** Still in the converse direction:

> Suppose $R/(a)$ is an integral domain, $a \neq 0$. Show that $a$ is **not a
> unit**.

Contrapositive again, and it is shorter than the one you just did: suppose $a$
*is* a unit and show $R/(a)$ fails to be an integral domain.

- *Unit:* $a$ with $ra = 1$ for some $r \in R$.
- $(a) = \{ra : r \in R\}$.
- $b + (a) = c + (a)$ exactly when $a \mid b - c$.
- *Integral domain:* commutative, $1 \neq 0$, no zero divisors.
- $R/(1) = R/R$ has one element — you saw this on the last card.

Start by working out what the set $(a)$ actually is when $a$ is a unit.
