---
kind: question
title: ch05-open-054
---
Five problems are in scope this sitting, and we start with 05.4. They are Garling 05.4, 05.6, 05.7, 05.8 and 05.10, already stated with empty solution regions in the chapter's write-up file `chapters/ch05-tests-for-irreducibility/homework/ch05-homework.tex`. The choice is deliberate: 05.7 is Eisenstein's criterion applied straight, 05.6 is Eisenstein turned upside down, 05.10 is Eisenstein over a bigger field than $\mathbb{Q}$, and 05.4 and 05.8 are the two in the set that need an idea the criterion does not give you. You are not answering 05.4 yet.

**Problem 05.4.** Suppose that $K$ is a field and that $f$ and $g$ are relatively prime in $K[x]$. Show that $f - yg$ is irreducible in $K(y)[x]$.

Everything that statement uses:

- $K[x]$ — polynomials in $x$ with coefficients in the field $K$.
- $K(y)$ — the rational functions in $y$ over $K$, that is, quotients of two polynomials in $y$.
- $K(y)[x]$ — polynomials in $x$ whose coefficients are rational functions in $y$.
- $K[x][y]$ — polynomials in $y$ whose coefficients are polynomials in $x$; the same expressions as above, sorted by the other letter.
- relatively prime — the only common divisors of $f$ and $g$ in $K[x]$ are the nonzero constants.
- unit of $K(y)[x]$ — a nonzero element of $K(y)$, since those are the invertible ones.
- irreducible — not a unit, and in every factorisation into two factors one of them is a unit.
- greatest common divisor in $K[x]$ — the common divisor of highest degree, taken monic.

One small thing first. The expression $f - yg$ carries two letters, so it sits in two rings at once, and the whole problem turns on which of them you look at it in. Take $K = \mathbb{Q}$, $f = x^3 + 1$ and $g = x + 2$, so
$$f - yg = x^3 + 1 - y(x+2).$$

Regard that as a polynomial in $y$ alone, with coefficients in $\mathbb{Q}[x]$. Write down its coefficients, and give their greatest common divisor in $\mathbb{Q}[x]$.
