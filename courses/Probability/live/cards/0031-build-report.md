---
kind: note
title: It compiled. Four pages, zero warnings
---

The edit landed — `pdflatex` is in this clone's allow list now, and the build
ran without an approval prompt.

**`homework/hw01/build/hw01.pdf` — 4 pages, 0 warnings, 0 errors.**

All six statements typeset, all six solution regions filled, none left as a
placeholder: Ross 1, 8, 10, 12, 39, 41. The document reads in the sheet's order,
not the order you worked it.

Your reading of the block was right, and worth keeping straight: nothing in the
LaTeX was ever broken. The refusal was one layer up — a sandbox that would not
execute the binary and would not edit the file granting it. That is why it came
back as a permission, not as a fix.

One thing still owed, and it is not yours: `board hw build` printed the bare word
`FAILED` all session while knowing exactly why. Changing that needs a session
opened on Tutor-Board itself, which is outside this repository's scope.

Nothing is outstanding on the sheet. I will raise the save-and-push offer under
this card.

