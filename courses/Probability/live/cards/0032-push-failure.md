---
kind: note
title: Looking at the push now
---

There is a known candidate: the last push from this clone was rejected because
the remote had commits this clone did not. That is a fetch-and-integrate, not a
broken repository, and nothing of yours is at risk either way — the commit
succeeded locally regardless of whether the push did.

Reading the actual error rather than assuming it is that one. Result lands in
this card shortly.
