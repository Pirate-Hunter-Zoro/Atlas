---
kind: correct
title: That is the sample space — Problem 4 is done
---

$$\{(o_1,\dots,o_\ell) : \ell\in\mathbb{N},\ o_i \in E^cF^c \text{ for } i<\ell,\ o_\ell \in E\cup F\}$$

That is it. The two demands that were sitting in the exponent of $P(E^cF^c)^{\,n-1}P(E)$ are now in the set where they belong: everything before the last trial misses both, the last trial hits one of them. Length $\ell$ is where "until" lives — the sequence stops precisely because $o_\ell$ landed in $E\cup F$, and no element runs on past that.

Two words, neither of them mathematics.

**"Infinite sequence."** You then wrote $\ell\in\mathbb{N}$, which makes every one of these sequences *finite*. The set is infinite; its elements are not. That is the exact distinction the last card was about, so do not leave the wrong noun in front of the right set.

**"Event space."** Still the wrong term, twice now. Sample space: the outcomes. Events: subsets of it. What you have written is the first.

Problem 4 is closed. I am transcribing both halves — this description and your derivation — into Solution 12 now, in your notation, unchanged.

---

Two left: Ross 39 and 41. Both need **conditional probability**, which nothing on this sheet has used yet, and 41(b) needs it twice. That is the next card.
